# Device simulator

`shared/telemetry-simulator.js` is marked “Simulación demo · no GPS real”. It can select a demo vehicle, start/pause/reset emissions, change speed, simulate signal loss, stopped vehicle, delay, and incident events. It emits local objects only; no network or paid provider is used.
