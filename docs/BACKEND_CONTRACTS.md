# BACKEND CONTRACTS

Shared JavaScript contracts live in `shared/contracts.js` and cover municipalities, vehicles, drivers, routes, route runs, positions, incidents, and citizen reports. They are backend-ready contracts, not proof of a deployed API.
