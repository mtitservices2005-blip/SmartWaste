# Registro de deuda técnica — SmartWaste Alpha

> Datos demo · no producción. Basado en `docs/CURRENT_STATE_AUDIT.md` (2026-07-29).

Severidad: **Alta** (bloquea el siguiente hito o representa riesgo de seguridad/tenancy), **Media** (debe resolverse antes de piloto real), **Baja** (limpieza / higiene, no bloqueante).

| # | Ítem | Severidad | Ubicación | Impacto | Esfuerzo estimado |
|---|---|---|---|---|---|
| 1 | RLS excluye al rol `driver` de insert/update en las 13 tablas gestionadas por el loop de políticas, incluida `vehicle_positions` | Alta | `supabase/migrations/202607150004_sw014_auth_rls_policies.sql:28,30` | Un conductor autenticado real no podría iniciar rutas, actualizar progreso ni enviar telemetría — rompe el flujo operativo central en cuanto se active Supabase real | Pequeño (añadir `'driver'` al array de roles permitidos, con columnas/condiciones que limiten qué puede tocar de cada tabla; requiere diseño, no solo el flag) |
| 2 | Sin política de escritura para ciudadanos anónimos en `citizen_reports` | Alta | `supabase/migrations/202607150004_sw014_auth_rls_policies.sql:24-31` | El portal ciudadano no podría persistir reportes reales vía Supabase — toda la feature de "Portal ciudadano" quedaría bloqueada en producción | Medio (definir rol/política `anon insert` con validaciones anti-abuso, o un endpoint de servidor intermedio) |
| 3 | `sw008_rls_draft.sql` habilita RLS sin políticas en 7 tablas; solo es seguro si `sw014` se aplica después en el mismo entorno, y no hay evidencia de que las 5 migraciones se hayan corrido juntas nunca | Alta | `supabase/migrations/202607150002_sw008_rls_draft.sql` | Riesgo de aplicar migraciones fuera de orden y dejar tablas completamente bloqueadas (o, si se editan mal, completamente abiertas) | Bajo (documentar orden obligatorio; considerar colapsar sw008+sw014 en una sola migración de RLS al activar Supabase) |
| 4 | `createSupabaseOperationsAdapter` escribe `vehicle_id`, `driver_id` y `progress` directamente en `routes`, columnas que no existen ahí — pertenecen a `route_runs`/`vehicle_assignments` (hallazgo de revisión del PR #7, confirmado por lectura del esquema) | Alta | `shared/operations-adapter.js:72,74,76` vs `supabase/migrations/202607150001_sw007_foundation.sql:9,11,12` | El adapter Supabase no puede ejecutar el ciclo real (asignar/progresar) contra el esquema migrado sin reconciliación; bloquea directamente el objetivo de SW-020 de ejercer un ciclo operativo real | Medio (alinear el adapter al modelo `routes`→`route_runs`→`vehicle_assignments`, preservando la máquina de estados; no tocar el esquema para acomodar el adapter tal como está) |
| 5 | ~~4 archivos de test están nombrados como si probaran RLS/integridad/E2E reales pero solo hacen matching de texto sobre SQL/JS estático~~ — resuelto: cada archivo ahora lleva un comentario de alcance explícito al inicio aclarando que es un chequeo estático y remitiendo a la cobertura real (`tests/rls-adversarial.test.mjs`, `tests/operational-cycle.test.mjs`) | Baja (resuelto) | `tests/rls-static.test.mjs`, `tests/operations-integrity-static.test.mjs`, `tests/e2e-demo.test.mjs`, `tests/e2e-v2.test.mjs` | Riesgo de falsa confianza mitigado por el comentario; no se renombraron los archivos para no romper referencias existentes en otros docs | — |
| 6 | ~~`shared/auth-context.js` no está conectado a `frontend/`~~ — resuelto de forma opt-in por `frontend/auth-gate.js` (ver `docs/FRONTEND_LOGIN_SETUP.md`); pendiente verificación interactiva en navegador contra Supabase local | Media → Baja (pendiente de verificación real) | `frontend/auth-gate.js`, `tests/auth-gate.test.mjs` (solo lógica pura, sin navegador) | Sin configurar `window.SMARTWASTE_SUPABASE_CONFIG`, el comportamiento sigue siendo el mismo de antes (cosmético, sin login) — no es una regresión. Con la config activada, falta confirmar en navegador real que el login y el gating visual funcionan como se espera | Bajo (verificación interactiva pendiente en LabPC, ver `docs/FRONTEND_LOGIN_SETUP.md`) |
| 7 | ~~`DeviceSimulator` importado en `app.js` pero nunca instanciado~~ — resuelto: import eliminado. Pendiente: `shared/citizen-portal.js` sigue sin ser importado por `app.js` (el portal ciudadano de la UI usa lógica inline duplicada, no `createDemoFolio`/`findSectorService`/`findIncidentStatus`) | Baja | `shared/citizen-portal.js` (sin referencias en `frontend/app.js`, solo en `tests/citizen-portal.test.mjs`) | El mapa ya no importa un módulo sin usar; el portal ciudadano de la UI sigue con lógica duplicada e inline — migrar `app.js` a usar `shared/citizen-portal.js` queda como trabajo futuro, no bloqueante | Bajo (migrar `app.js` a usar `shared/citizen-portal.js` como fuente única de verdad) |
| 8 | Sin CI/CD — no existe `.github/workflows/` ni `package.json` en ningún punto del repo | Media | raíz del repo | Los 13 tests locales solo se ejecutan manualmente, y solo si se invocan como procesos separados (`node tests/*.test.mjs` sin loop no los ejecuta todos); nada impide un commit que rompa un test | Bajo–Medio (agregar un workflow simple que corra cada archivo de `tests/` como proceso independiente) |
| 9 | ~~`docs/DATA_MODEL_V2.md` es el único doc de dominio/datos sin banner "Datos demo · no producción"~~ — resuelto: banner agregado | Baja (resuelto) | `docs/DATA_MODEL_V2.md` | — | — |
| 10 | ~~`supabase/README.md` desactualizado — sigue diciendo "Pendiente crear migraciones..." cuando ya existen 5 migraciones~~ — resuelto: reescrito para reflejar las 6 migraciones aplicadas y verificadas en SW-020 | Baja (resuelto) | `supabase/README.md` | — | — |
| 11 | ~~Botón "Onboarding demo" en Master Admin sin handler~~ — ya no aplica: el handler existe (`frontend/app.js:179-180`, agregado en el PR de vista de supervisor, commit `a697ac9`); este ítem quedó desactualizado respecto al código | Baja (resuelto) | `frontend/app.js:179-180` | — | — |
| 12 | ~~Vista "supervisor" no existe como componente propio~~ — resuelto: `renderSupervisor()` (`frontend/app.js:87-93`) es una sección propia (`#supervisor`), con enlace de nav dedicado (`frontend/index.html:11`) y acciones reales (verificar ruta, resolver incidencia). Pendiente: "vista móvil de conductor" sigue sin existir como componente propio — es un `<div class="mobile">` de ~4 líneas embebido dentro de `renderMunicipal()` (`frontend/app.js:85`), sin sección ni enlace de nav propios | Media (parcialmente resuelto) | Supervisor: `frontend/app.js:87-93,127`, `frontend/index.html:11`. Conductor móvil: `frontend/app.js:85` (bloque `<div class="mobile">` dentro de `renderMunicipal()`) | Brecha entre lo prometido (README: "vista móvil conductor" como feature propia) y lo construido — sigue siendo solo un widget demo dentro del panel municipal, no una vista dedicada | Medio (construir la vista móvil de conductor como componente propio es trabajo de producto, no deuda de "arreglo rápido") |
| 13 | Numeración de hitos SW-001–SW-019 no es trazable 1:1 a commits — 5 números solo existen como nombres de archivo SQL, 7 números no existen en ningún lado | Baja (proceso, no código) | Historial git completo, ver `docs/CURRENT_STATE_AUDIT.md` | Aclarado por el Project Owner: 6 PR agrupan los 19 hitos funcionales — riesgo de comunicación queda mitigado si se cita siempre "6 PR / 19 hitos funcionales" juntos | Trivial (ya reflejado en `docs/CURRENT_STATE_AUDIT.md`) |
| 14 | Supabase Realtime (`postgres_changes`) no entrega eventos de `vehicle_positions` pese a estar correctamente configurado — confirmado con evidencia real de CI (PR #17), no solo revisión de código | Alta | `supabase/migrations/202607150007_sw016_telemetry_realtime.sql`, `tests/telemetry-persistence.test.mjs` (antes `tests/telemetry-realtime.test.mjs`) | El canal llega a `SUBSCRIBED` pero nunca recibe el `INSERT`, tanto con un cliente con rol `driver` como con `service_role` (descarta RLS/JWT como causa). Los logs del propio contenedor de Realtime (capturados en CI) muestran que solo arranca su conexión de replicación interna para "Broadcast Changes" (`realtime.messages`) — nunca ninguna para `vehicle_positions`/`supabase_realtime`, ni siquiera tras otorgar `SELECT` a `supabase_realtime_admin` (el rol que debería leer el WAL, según investigación externa). La causa raíz sigue sin identificarse; puede requerir acceso directo a Postgres/al código fuente de Realtime (LabPC) para diagnosticar más allá de lo que permiten los logs del job de CI. `shared/integration/status.json`'s `sw016.realtime` se mantiene `REAL_NOT_RUN` — la persistencia real (ingesta + rechazo RLS de escritura) sí quedó `VERIFIED_REAL` en el mismo PR | Medio–Alto (requiere depuración a nivel de Postgres/Realtime con más herramientas que las disponibles vía CI de GitHub Actions) |
| 15 | El contenedor de auditoría/Claude Code no tiene salida de red hacia el CDN de blobs de Docker Hub (`production.cloudfront.docker.com`) — el proxy de egreso de la organización responde `403 Forbidden` a ese host específico. `docker pull` puede resolver el manifest (`registry-1.docker.io` funciona) pero falla al bajar las capas de imagen. Confirmado con `docker info`/`docker pull hello-world` y con `curl $HTTPS_PROXY/__agentproxy/status` (`recentRelayFailures`: `connect_rejected`, `gateway answered 403 to CONNECT`, host `production.cloudfront.docker.com:443`) | Alta | Entorno del contenedor (no es código del repo); ver `/root/.ccr/README.md`, sección "403 / 407 from the proxy" | Bloquea `supabase start` (Postgres/GoTrue/PostgREST/Kong son imágenes Docker) desde este contenedor — SW-016 (telemetría/Realtime) y SW-017 (E2E frontend real contra Supabase local) no se pueden ejecutar aquí, igual que SW-020 tuvo que ejecutarse en LabPC. No es un problema de configuración de Docker/CA — es una denegación de política de red explícita; no reintentar ni rodear, según el README del proxy | No accionable desde el repo/código. Requiere que alguien con acceso a la política de egreso del workspace habilite `production.cloudfront.docker.com` (y probablemente otros hosts de CDN de imágenes) en el proxy, o que el hito se siga ejecutando en LabPC como hasta ahora |

## No incluido en este registro

Elementos que el propio código ya se auto-declara como pendiente de forma explícita (p. ej. cada "REAL_NOT_RUN" en `shared/integration/status.json`, o cada "no upload real" en el portal ciudadano) no se listan aquí como deuda individual — son decisiones de alcance de la Alpha, documentadas, no bugs ni atajos ocultos. El ítem #12 es la excepción porque hay una discrepancia entre lo prometido (README) y lo construido.
