# Registro de deuda técnica — SmartWaste Alpha

> Datos demo · no producción. Basado en `docs/CURRENT_STATE_AUDIT.md` (2026-07-29).

Severidad: **Alta** (bloquea el siguiente hito o representa riesgo de seguridad/tenancy), **Media** (debe resolverse antes de piloto real), **Baja** (limpieza / higiene, no bloqueante).

| # | Ítem | Severidad | Ubicación | Impacto | Esfuerzo estimado |
|---|---|---|---|---|---|
| 1 | ~~RLS excluye al rol `driver` de insert/update en las 13 tablas gestionadas por el loop de políticas, incluida `vehicle_positions`~~ — resuelto: `supabase/migrations/202607150006_sw020_rls_fixes.sql` ("Gap 1") agrega `driver_update_own_route_run` y `driver_insert_own_vehicle_position`/`driver_insert_own_incident`, políticas acotadas al propio `route_run`/vehículo asignado/incidencia del conductor, no un blanket grant | Alta (resuelto) | `supabase/migrations/202607150006_sw020_rls_fixes.sql:20-65` | Verificado con evidencia real: `tests/rls-adversarial.test.mjs` y `tests/telemetry-persistence.test.mjs` (ambos en CI) ejercen estas políticas contra Supabase local real, incluyendo el rechazo de escritura para un vehículo no asignado | — |
| 2 | ~~Sin política de escritura para ciudadanos anónimos en `citizen_reports`~~ — resuelto: `supabase/migrations/202607150006_sw020_rls_fixes.sql` ("Gap 2") agrega `anon_insert_citizen_report`, acotada por `municipality_is_onboarded` | Alta (resuelto) | `supabase/migrations/202607150006_sw020_rls_fixes.sql:71-99` | El portal ciudadano ya puede persistir reportes reales vía Supabase sin necesidad de un endpoint intermedio | — |
| 3 | ~~`sw008_rls_draft.sql` habilita RLS sin políticas en 7 tablas; solo es seguro si `sw014` se aplica después en el mismo entorno~~ — mitigado (SW-038): combinar retroactivamente `sw008`/`sw014` en una sola migración atómica no es viable (ambas ya están aplicadas en cualquier entorno existente; reescribir migraciones ya shippeadas rompe el tracking de `supabase_migrations.schema_migrations`). En su lugar: `tests/rls-coverage.test.mjs` (nuevo, corre en el job `integration-tests` de CI en cada PR) verifica contra Postgres real que ninguna tabla de `public` tenga RLS habilitado sin al menos una política — detecta el hueco automáticamente si una migración futura lo repite, en vez de fallar en silencio. `supabase/README.md` documenta la recuperación: un despliegue interrumpido entre `sw008`/`sw014` se arregla simplemente volviendo a correr las migraciones (la CLI de Supabase solo re-corre las pendientes) | Media (mitigado — detección automática + recuperación documentada; no existe una migración atómica que combine ambos archivos, ni tiene sentido crearla retroactivamente) | `tests/rls-coverage.test.mjs`, `supabase/README.md`, `supabase/migrations/202607150002_sw008_rls_draft.sql` | El riesgo estructural (una migración puede fallar a mitad de un deploy) no desaparece — ningún sistema de migraciones incrementales lo elimina del todo — pero ahora es detectable automáticamente en CI y la recuperación es un paso documentado de un comando, no una investigación | — |
| 4 | ~~`createSupabaseOperationsAdapter` escribía `vehicle_id`, `driver_id` y `progress` directamente en `routes` (no existen ahí); `transitionRouteRun()` descartaba el valor numérico de `progress` en silencio porque `route_runs` no tenía columna para él~~ — **resuelto (SW-030)**: `supabase/migrations/202607150010_sw030_route_run_progress.sql` agrega `route_runs.progress` (con constraint `between 0 and 100` y backfill a 100 para runs ya completados/verificados); `transitionRouteRun()` (`shared/operations-adapter.js`) ahora escribe `{ status: next, ...patch }`, incluyendo `progress` en `updateProgress`/`completeRoute`. Encontrado ya resuelto en el código al revisar SW-038 — el registro de deuda nunca se había actualizado para reflejarlo | Alta (resuelto) | `supabase/migrations/202607150010_sw030_route_run_progress.sql`, `shared/operations-adapter.js` (`transitionRouteRun`) | Verificado con evidencia real: `tests/operational-cycle.test.mjs` (corre en el job `integration-tests` de CI en cada PR, contra Supabase local real) asserta explícitamente `progressed.data.progress === 50` tras `updateProgress(routeId, 50)` y `completed.data.progress === 100` tras completar — ya no hay pérdida silenciosa | — |
| 5 | ~~4 archivos de test están nombrados como si probaran RLS/integridad/E2E reales pero solo hacen matching de texto sobre SQL/JS estático~~ — resuelto: cada archivo ahora lleva un comentario de alcance explícito al inicio aclarando que es un chequeo estático y remitiendo a la cobertura real (`tests/rls-adversarial.test.mjs`, `tests/operational-cycle.test.mjs`) | Baja (resuelto) | `tests/rls-static.test.mjs`, `tests/operations-integrity-static.test.mjs`, `tests/e2e-demo.test.mjs`, `tests/e2e-v2.test.mjs` | Riesgo de falsa confianza mitigado por el comentario; no se renombraron los archivos para no romper referencias existentes en otros docs | — |
| 6 | ~~`shared/auth-context.js` no está conectado a `frontend/`~~ — resuelto de forma opt-in por `frontend/auth-gate.js` (ver `docs/FRONTEND_LOGIN_SETUP.md`); pendiente verificación interactiva en navegador contra Supabase local | Media → Baja (pendiente de verificación real) | `frontend/auth-gate.js`, `tests/auth-gate.test.mjs` (solo lógica pura, sin navegador) | Sin configurar `window.SMARTWASTE_SUPABASE_CONFIG`, el comportamiento sigue siendo el mismo de antes (cosmético, sin login) — no es una regresión. Con la config activada, falta confirmar en navegador real que el login y el gating visual funcionan como se espera | Bajo (verificación interactiva pendiente en LabPC, ver `docs/FRONTEND_LOGIN_SETUP.md`) |
| 7 | ~~`DeviceSimulator` importado en `app.js` pero nunca instanciado~~ — resuelto: import eliminado. ~~`shared/citizen-portal.js` sin importar por `app.js`~~ — resuelto: `frontend/app.js` ahora importa `createDemoFolio`/`findSectorService`/`findIncidentStatus` y los usa en los 3 puntos donde antes había lógica inline duplicada (selector de sector, búsqueda de folio, generación de folio al reportar incidencia) | Baja (resuelto) | `frontend/app.js` (handlers de `#citizenSector`, `checkFolio`, `#incidentForm`), `shared/citizen-portal.js` | Verificado en navegador (Playwright): selector de sector, búsqueda de folio conocido/desconocido y generación secuencial de folios (`SW-FOLIO-3001`, `SW-FOLIO-3002`, ...) funcionan usando el módulo compartido como única fuente de verdad | — |
| 8 | ~~Sin CI/CD~~ — resuelto: `.github/workflows/tests.yml` (agregado en el PR de limpieza de deuda técnica) corre dos jobs en cada PR/push a `main`: `unit-tests` (cada `tests/*.test.mjs` sin Supabase, como proceso independiente) e `integration-tests` (con Supabase local real vía Docker) | Media (resuelto) | `.github/workflows/tests.yml`, `package.json` | En uso activo desde entonces en todos los PRs (#16–#20) — un commit que rompa un test ya bloquea el merge visualmente en CI | — |
| 9 | ~~`docs/DATA_MODEL_V2.md` es el único doc de dominio/datos sin banner "Datos demo · no producción"~~ — resuelto: banner agregado | Baja (resuelto) | `docs/DATA_MODEL_V2.md` | — | — |
| 10 | ~~`supabase/README.md` desactualizado — sigue diciendo "Pendiente crear migraciones..." cuando ya existen 5 migraciones~~ — resuelto: reescrito para reflejar las 6 migraciones aplicadas y verificadas en SW-020 | Baja (resuelto) | `supabase/README.md` | — | — |
| 11 | ~~Botón "Onboarding demo" en Master Admin sin handler~~ — ya no aplica: el handler existe (`frontend/app.js:179-180`, agregado en el PR de vista de supervisor, commit `a697ac9`); este ítem quedó desactualizado respecto al código | Baja (resuelto) | `frontend/app.js:179-180` | — | — |
| 12 | ~~Vista "supervisor" no existe como componente propio~~ — resuelto. ~~"Vista móvil de conductor" sin componente propio~~ — resuelto: ahora es su propia `<section id="conductor">` (`renderDriverSection()`, `frontend/app.js`), con enlace de nav dedicado (`frontend/index.html`) y gateada en `frontend/auth-gate.js`'s `SECTION_ROLES` a los mismos roles que ya veían Municipal (`municipal_admin`/`dispatcher`/`driver`). El contenido (`renderDriverMobile()`, mapa del conductor, progreso de paradas) no cambió, solo se movió de estar embebido dentro de `renderMunicipal()` a ser una sección de primer nivel | Media (resuelto) | `frontend/app.js` (`renderDriverSection()`, `showSection()`), `frontend/index.html`, `frontend/auth-gate.js` (`SECTION_ROLES`) | `tests/auth-gate.test.mjs` actualizado y en verde; verificado con Playwright que la pestaña "Conductor" cambia sola, el mapa del conductor se ve e `invalidateSize()` corre al entrar (ya no depende de entrar a Municipal) | — |
| 13 | Numeración de hitos SW-001–SW-019 no es trazable 1:1 a commits — 5 números solo existen como nombres de archivo SQL, 7 números no existen en ningún lado | Baja (proceso, no código) | Historial git completo, ver `docs/CURRENT_STATE_AUDIT.md` | Aclarado por el Project Owner: 6 PR agrupan los 19 hitos funcionales — riesgo de comunicación queda mitigado si se cita siempre "6 PR / 19 hitos funcionales" juntos | Trivial (ya reflejado en `docs/CURRENT_STATE_AUDIT.md`) |
| 14 | Supabase Realtime (`postgres_changes`) no entrega eventos de `vehicle_positions` de forma confiable — investigado en profundidad en SW-022 con acceso directo a Postgres y al log del contenedor de Realtime; el fallo resultó ser no determinístico, no un bug simple con una única causa | Alta | `supabase/migrations/202607150007_sw016_telemetry_realtime.sql`, `shared/telemetry-simulator.js` (`subscribe()`), `tests/telemetry-persistence.test.mjs`, `tests/telemetry-realtime-diagnosis.test.mjs` | SW-022 (PR #20) inspeccionó Postgres directamente (publicación `supabase_realtime`, replication slots, tabla `realtime.subscription`, grants) y confirmó que toda la configuración del lado servidor está correcta: `vehicle_positions` sí está en la publicación, el slot de replicación (`cainophile_...`, plugin `pgoutput`) está activo, y la fila de suscripción se registra con el filtro y el JWT correctos. Descarta RLS/JWT/config como causa. Pese a eso, 3 corridas de CI con la misma rama, mismo código, mismo entorno, mostraron 3 comportamientos distintos: (1) el evento se entregó correctamente esperando ~2-3s después de que el canal confirma `SUBSCRIBED`; (2) el servidor cerró el canal (`CLOSED`) ~1.3s después de ese mismo ack; (3) el canal se quedó abierto y nunca reportó ningún error, pero el evento simplemente nunca llegó. El caso (3) es clave: no hay ninguna señal observable desde el cliente para reintentar sobre ella — el contador interno `subscription_errors` del contenedor de Realtime se incrementa en los 3 casos casi al mismo tiempo (~1-1.3s tras el join), pero solo a veces eso se traduce en un `CLOSED` visible para el cliente. `shared/telemetry-simulator.js`'s `subscribe()` reintenta con backoff (1s/2s/4s) ante fallos observables como mitigación de mejor esfuerzo, pero no puede resolver el caso (3) por diseño — se documenta así en el propio código. La aserción de entrega de Realtime en `tests/telemetry-persistence.test.mjs` se mantiene fuera (no bloqueante); `shared/integration/status.json`'s `sw016.realtime` se mantiene `REAL_NOT_RUN`. `tests/telemetry-realtime-diagnosis.test.mjs` queda en CI como diagnóstico permanente (no bloqueante) para seguir recolectando evidencia en corridas futuras | Alto (parece ser un problema de la propia infraestructura self-hosted de Realtime en esta versión, `v2.113.4` — no resoluble solo con cambios de configuración o lógica del lado cliente; requeriría investigar el código fuente de Realtime o probar una versión distinta del contenedor) |
| 15 | El contenedor de auditoría/Claude Code no tiene salida de red hacia el CDN de blobs de Docker Hub (`production.cloudfront.docker.com`) — el proxy de egreso de la organización responde `403 Forbidden` a ese host específico. `docker pull` puede resolver el manifest (`registry-1.docker.io` funciona) pero falla al bajar las capas de imagen. Confirmado con `docker info`/`docker pull hello-world` y con `curl $HTTPS_PROXY/__agentproxy/status` (`recentRelayFailures`: `connect_rejected`, `gateway answered 403 to CONNECT`, host `production.cloudfront.docker.com:443`) | Alta | Entorno del contenedor (no es código del repo); ver `/root/.ccr/README.md`, sección "403 / 407 from the proxy" | Bloquea `supabase start` (Postgres/GoTrue/PostgREST/Kong son imágenes Docker) desde este contenedor — SW-016 (telemetría/Realtime) y SW-017 (E2E frontend real contra Supabase local) no se pueden ejecutar aquí, igual que SW-020 tuvo que ejecutarse en LabPC. No es un problema de configuración de Docker/CA — es una denegación de política de red explícita; no reintentar ni rodear, según el README del proxy | No accionable desde el repo/código. Requiere que alguien con acceso a la política de egreso del workspace habilite `production.cloudfront.docker.com` (y probablemente otros hosts de CDN de imágenes) en el proxy, o que el hito se siga ejecutando en LabPC como hasta ahora |
| 16 | ~~SW-027 ("Crear ruta") conecta una ruta recién creada mutando en runtime el diccionario `routePaths`~~ — resuelto por completo. `shared/telemetry-simulator.js`'s `DeviceSimulator` ahora acepta un `getPath` opcional en el constructor (`{ getPath: () => [...] }`), llamado de forma perezosa en cada `emit()` en vez de leído una sola vez — sin `getPath`, sigue el mismo fallback de siempre (`routePaths`/`trucks`), así que ningún llamador existente cambia de comportamiento. `frontend/app.js` pasa `getPath: () => routeGeometry(truck.routeId)` en sus dos puntos de construcción (seeding inicial y `registerDriverSimulator()`), y también se corrigió `startSimulation()`'s tick, que todavía leía `routePaths[truck.routeId]` directamente (se me había pasado en SW-033). Con esto, `finishCreateRoute()` ya no necesita mutar `routePaths` en absoluto — se eliminó esa línea | Baja (resuelto) | `shared/telemetry-simulator.js` (`DeviceSimulator`), `frontend/app.js` (`registerDriverSimulator()`, seeding de `driverSimulators`, `startSimulation()`) | Verificado: `tests/telemetry-simulator.test.mjs` cubre que `getPath()` gana sobre el fallback y se llama en cada `emit()`, no solo al construir; suite completa sin regresiones; Playwright confirma que dibujar una ruta, asignarle vehículo e iniciar la simulación funciona sin errores | — |
| 17 | ~~SW-034 conectó las escrituras de `frontend/app.js` a Supabase real, pero no hidrataba datos reales preexistentes hacia la página en ejecución~~ — resuelto en dos fases (SW-035). **Fase A:** `hydrateVehiclesAndDrivers()` trae `listVehicles()`/`listDrivers()` y los agrega a `trucks`/`drivers`. **Fase B:** `hydrateRoutes()` trae `listRoutes()` + el nuevo `listRouteRuns()` (`shared/operations-adapter.js` — no existía ningún método que leyera `route_runs` en bloque, solo transicionarlos uno por uno), cruza cada ruta con su `route_run` más reciente por `route_id` para resolver vehículo/chofer/progreso reales (routes en sí no tiene esas columnas — viven en `route_runs`), y siembra geometría/paradas en el adaptador demo con el mismo patrón que ya siembra las 5 rutas demo al inicio del módulo, para que `routeGeometry()`/`drawMapLayers()`/la vista del conductor funcionen igual que con una ruta demo. Una ruta hidratada con vehículo asignado también recibe `simState`/`driverSimulators` para poder participar de la simulación | Media (resuelto) | `frontend/app.js` (`hydrateVehiclesAndDrivers()`, `hydrateRoutes()`, `bootstrapRealBackend()`); `shared/operations-adapter.js` (`listRouteRuns()`, nuevo) | Verificado con un cliente Supabase simulado en Playwright: una ruta, un vehículo y un chofer sembrados directamente en el mock (nunca creados en la sesión del navegador) aparecen al cargar la página, con la unidad asignada y el progreso reales correctos (leídos de `route_runs`), sin errores de consola. `tests/operations-adapter.test.mjs` cubre `listRouteRuns()` con un cliente simulado | — |

| 18 | "Trazado inteligente" (`finishCreateRoute()` en `frontend/app.js`, `shared/osrm-routing.js`) usa el servidor demo público de OSRM (`router.project-osrm.org`) para ajustar el trazo dibujado a calles reales — elegido explícitamente por el Project Owner para un piloto de 3-5 camiones. Es gratuito y no requiere credenciales, pero es un servicio de evaluación: sin SLA, con límites de tasa no documentados públicamente, no pensado para carga de producción | Media | `shared/osrm-routing.js`, `frontend/app.js` (`finishCreateRoute()`) | Si el servidor demo público limita o rechaza peticiones bajo uso real, la función cae automáticamente al trazo en línea recta (fallback ya implementado, nunca bloquea la creación de rutas — regla 5); el mensaje de estado en la UI ya distingue ambos casos | Bajo mientras el piloto sea pequeño; si el municipio crece o el uso se vuelve intensivo, migrar a un servidor OSRM propio o a un proveedor con SLA (ver comparación de servicios de ruteo hecha en la sesión de planeación de este hito) |

| 19 | El optimizador de orden de paradas (`shared/route-optimizer.js`, roadmap ítem 2, opt-in vía el checkbox "Optimizar orden de paradas" en "Crear ruta") usa distancia en línea recta (haversine) entre puntos, no la red vial real — no considera calles de un solo sentido, restricciones de giro, ni el trazo que luego calcula OSRM. El orden se decide *antes* de llamar a OSRM (por diseño: OSRM ajusta la geometría del orden ya decidido a calles reales, no vuelve a decidir el orden), así que el resultado puede no ser el orden verdaderamente más corto una vez ajustado a calles reales. Es además una heurística (nearest-neighbor + 2-opt), no una solución exacta de TSP — adecuada para las 2-30 paradas dibujadas a mano por un despachador, no garantiza el óptimo global | Baja | `shared/route-optimizer.js`, `frontend/app.js` (`finishCreateRoute()`, checkbox `#createRouteOptimize`) | El trazo sigue siendo válido y usable en cualquier caso (la optimización es opt-in y desactivada por defecto — regla 5); es una mejora de eficiencia de ruta, no de correctitud, y el despachador puede desactivarla si el orden manual es intencional (p. ej. por calles de un solo sentido) | Bajo si en el futuro se quisiera reoptimizar contra distancias reales de calle (usar el servicio Trip/Table de OSRM en vez de solo Route), pero fuera de alcance de este hito |

| 20 | ~~"GPS real" ... pendiente verificación interactiva~~ — **VERIFIED_REAL** (2026-08-13): roadmap ítem 3 (`shared/browser-geolocation.js`, botón opt-in "Compartir mi ubicación real" en la vista Conductor) persiste la posición real del conductor en `vehicle_positions` (`source: browser_geolocation`). SW-036: `frontend/app.js` hace polling (`fetchRealPositions()`/`OPS_GPS_POLL_INTERVAL_MS`, no Realtime — ver ítem #14) contra `shared/operations-adapter.js`'s `listLatestPositions()`, y muestra la posición real (con insignia visual distintiva, `truckIcon(truck, isRealGps)`) en el mapa de Operaciones cuando está fresca (`REAL_GPS_FRESHNESS_MS`, umbral contra `captured_at` — no hay campo de estado "sigo compartiendo" persistido, así que sin esta regla una posición vieja se mostraría como vigente para siempre). Camiones sin GPS real activo, o con posición vencida, siguen 100% simulados, sin cambio de comportamiento. Depende de permiso de ubicación del navegador/teléfono del conductor — sin ese permiso, o en un navegador sin soporte de Geolocation API, el botón falla con un mensaje de estado explicativo y no rompe el resto de la vista | Baja (resuelto — verificado interactivamente) | `shared/browser-geolocation.js`, `shared/operations-adapter.js` (`listLatestPositions()`), `frontend/app.js` (`startDriverGps()`/`stopDriverGps()`, `fetchRealPositions()`/`startOpsGpsPolling()`/`drawMapLayers()`, vista Conductor y mapa de Operaciones) | Verificado en LabPC del Project Owner contra Supabase local real (Docker), no solo mocks: `node scripts/seed-local.mjs` (con `vehicle_assignments` enlazando chofer↔vehículo) + login real por rol vía `frontend/auth-gate.js`, chofer activó "Compartir mi ubicación real" (posición simulada vía DevTools > Sensors, permiso de ubicación del navegador concedido), la posición llegó a `vehicle_positions` real, y a los ~8s el mapa de Operaciones del despachador mostró la insignia de GPS real (●) sobre el vehículo correspondiente. También corrigió en el camino un bug real encontrado durante esta verificación: `startDriverGps()` mandaba `correlation_id: 'gps-<timestamp36>'`, no un UUID válido, y la columna es `uuid` — cada inserción real fallaba con "invalid input syntax for type uuid" hasta que se quitó ese campo (la base genera el UUID por default) | — |

| 21 | ~~"Reoptimización dinámica" (roadmap ítem 4, final) se entregó como **sugerencia de vista previa, no persistida**: el botón "Reoptimizar ruta" en `renderRouteDetail()` (`frontend/app.js`) calcula un nuevo orden de las paradas pendientes vía `shared/route-reoptimizer.js` (reutiliza `optimizeWaypointOrder()`, ítem 2, desde la posición actual del camión) y lo muestra en el panel, pero nunca escribe a `route_stops`/`route_paths` — decisión de alcance confirmada con el Project Owner, no un atajo oculto.~~ Corrección (revisión de Codex en PR #46, 3 hallazgos, los 3 resueltos): (1) el "pendiente" se aproximaba como la cola de la lista de paradas más allá de `route.progress`%, lo cual podía estar francamente equivocado (no solo impreciso) cuando el progreso no coincidía con qué paradas se recolectaron de verdad — resuelto: `previewRouteReoptimization()` ahora deriva el estado real por-parada con el mismo mecanismo que ya usa la vista del conductor (`positionHistory`/`deriveStopStatus()`, poblado para todo camión con ruta vía `registerDriverSimulator()`, incluidas las rutas hidratadas desde Supabase), en vez de asumir que las paradas recolectadas forman un prefijo proporcional al progreso; (2) un `Map` indexado por coordenada en `shared/route-reoptimizer.js` colapsaba paradas distintas que comparten la misma coordenada (p. ej. una ruta cerrada que vuelve al depósito), perdiendo una parada y duplicando otra en la sugerencia — resuelto consumiendo una cola por coordenada en vez de sobrescribir; (3) la heurística nearest-neighbor+2-opt solo mejora sobre su propia semilla y nunca se comparaba contra el orden original, así que podía sugerir un orden más largo que el actual — resuelto devolviendo el orden original cuando el resultado optimizado no es mejor. Los 3 con test de regresión que falla sin el fix correspondiente | Media (resuelto) | `shared/route-reoptimizer.js`, `frontend/app.js` (`previewRouteReoptimization()`, `renderRouteDetail()`) | Verificado: `tests/route-reoptimizer.test.mjs` cubre los 3 fixes (coordenadas duplicadas, orden nunca peor que el original) con casos que fallan sin el fix; Playwright confirma que la sugerencia sigue funcionando con estado real por-parada, sin persistencia ni riesgo al motor de simulación | Sigue pendiente (fuera de alcance de este hito, confirmado): aplicar la sugerencia (escribir el nuevo orden) requiere resolver dos huecos reales: (1) `saveRouteStops()`/`savePathPoints()` en `shared/operations-adapter.js` son solo-inserción en ambos adaptadores — falta `replace`/`delete` para reescribir sin duplicar filas; (2) `route.covered`/`route.pending` necesitarían re-derivarse de un estado real por-parada (o eliminarse) en vez de seguir siendo conteos estáticos |

| 22 | "Estimación de viviendas" (roadmap ítem 5, `shared/overpass-buildings.js`, `finishCreateRoute()`) estima cuántas viviendas sirve una ruta contando huellas de edificios de OpenStreetMap dentro de un buffer fijo (30m por defecto) alrededor de los puntos de la ruta, vía la instancia pública de Overpass (`overpass-api.de`) — gratuita, sin credenciales, pero sin SLA y con límites de uso justo (mismo tipo de decisión que OSRM, ítem #18). **No es un catastro real**: la precisión depende por completo de qué tan completa esté la cobertura de edificios de OSM para la zona del municipio piloto — puede subestimar o sobrestimar donde OSM esté incompleto. El rango de 2-3 minutos por vivienda para el tiempo estimado también es una suposición, no un dato medido en campo | Media | `shared/overpass-buildings.js`, `frontend/app.js` (`finishCreateRoute()`, `renderRouteDetail()`) | Si el servidor público de Overpass limita o rechaza peticiones, o la cobertura de OSM es pobre en la zona, la función cae a "estimación de viviendas no disponible" (fallback ya implementado, nunca bloquea la creación de rutas — regla 5); verificado con Playwright (endpoint mockeado, casos éxito y fallo) | Bajo mientras el piloto sea pequeño y la cobertura de OSM sea aceptable; catastro municipal real (datos GIS/direcciones provistos por el municipio) es la mejora de precisión ya identificada y confirmada como alcance futuro por el Project Owner, no construida en este hito |
| 23 | SW-039 conectó el portal ciudadano a Supabase real: `submitCitizenReport()` (`shared/citizen-portal.js`) inserta el reporte y sube evidencia real a un bucket privado (`citizen-evidence`, migración `202607150011_sw039_citizen_evidence_storage.sql`) cuando `SMARTWASTE_SUPABASE_CONFIG.municipality_id` está configurado; demo sin cambios si no. En la misma auditoría (ítem 3 del alcance de SW-039) se encontraron y corrigieron dos flujos que nunca mirroreaban a Supabase (el botón "Verificar" de Supervisor no llamaba a ningún adapter, ni siquiera el demo). **Quedan dos huecos conocidos, no construidos en este hito:** (1) el reporte real nunca lleva `sector_id` — el `<select>` del formulario usa los sectores demo hardcodeados de `shared/demo-data.js`, cuyos ids no existen como filas reales en Supabase (violarían el FK); (2) "Marcar resuelta" una incidencia sigue siendo 100% demo local — no existe ningún método `resolveIncident`/similar en `shared/operations-adapter.js` para ninguno de los dos adapters todavía | Media | `shared/citizen-portal.js`, `supabase/migrations/202607150011_sw039_citizen_evidence_storage.sql`, `frontend/app.js` (`verifyRouteManually()`, el submit de `#incidentForm`, `renderSupervisor()`) | Verificado con Playwright (cliente Supabase simulado, incluido `storage.from().upload()`): un reporte anónimo sin sesión (ruta "skip to public") inserta correctamente con `evidence_path` cuando hay archivo adjunto, cero errores de consola; modo demo sin `SMARTWASTE_SUPABASE_CONFIG` queda byte-idéntico (regla 5). El aviso en el panel de Supervisor ya distingue qué acción es real y cuál sigue siendo demo | Sectores reales: hace falta hidratar `sectors` desde Supabase (SW-035 solo hidrata vehículos/choferes/rutas, nunca sectores) antes de poder ofrecerlos en el formulario ciudadano. Resolución de incidencias real: agregar un método `resolveIncident`/`updateIncidentStatus` a `shared/operations-adapter.js` (la política `tenant_update_staff` ya cubre `incidents`, no hace falta migración nueva) |

| 23 | SW-040 (activación en staging real, Vercel + Supabase remoto) encontró 3 bugs reales que no se manifestaban en local, todos ya resueltos: (a) `scripts/build-frontend-config.mjs` copiaba `frontend/` a `dist/` para el build de Vercel pero nunca `shared/` — los imports estáticos `../shared/...` de `app.js`/`auth-gate.js` devolvían 404 en el hosting, y el módulo entero fallaba en la primera línea sin ningún error visible al usuario (sin login, sin botones, solo la barra superior estática); (b) `frontend/auth-gate.js` importaba `@supabase/supabase-js` en tiempo de ejecución desde `esm.sh` — cuando ese CDN externo no era alcanzable desde la red del visitante (firewall corporativo, etc.), fallaba con el mismo síntoma exacto que (a); (c) no existía ningún botón de "Cerrar sesión", haciendo imposible probar con varios roles sin borrar el storage del navegador a mano | Alta (resuelto) | `scripts/build-frontend-config.mjs`, `frontend/auth-gate.js`, `frontend/vendor/supabase-js.mjs` (nuevo, bundle autocontenido vía esbuild), `frontend/styles.css` (`.auth-logout`) | (a) y (b) son el mismo tipo de fallo (un import estático que no resuelve mata el módulo completo en silencio) con dos causas distintas — ninguna se detecta abriendo `frontend/index.html` localmente, solo en un deploy hosteado real; verificado por el Project Owner contra `https://smartwaste-staging.vercel.app` tras cada fix. (c) verificado permitiendo probar login como `municipal_admin`, `driver` y volver, sin tocar DevTools | — |
| 24 | ~~No existe ninguna pantalla en la interfaz para asignar un chofer a un vehículo~~ — **resuelto (SW-042)**: `shared/operations-adapter.js` agrega `listVehicleAssignments()`/`assignDriverToVehicle()` en ambos adaptadores (real: dos `update()` que terminan cualquier asignación `assigned` previa por vehículo y por chofer, luego un `insert()`; demo: deriva/muta `truck.driverId` directamente). `frontend/app.js` agrega el control "Asignar chofer"/"Reasignar chofer" en `renderTruckDetail()`, filtrando el selector a choferes del mismo tipo (real/demo) que el vehículo. También corrige el bug cosmético relacionado: `hydrateVehiclesAndDrivers()` ahora resuelve `driverId` desde `listVehicleAssignments()` en vez de hardcodear `null` | Media (resuelto) | `shared/operations-adapter.js` (`listVehicleAssignments`, `assignDriverToVehicle`), `frontend/app.js` (`renderTruckDetail()`, `driverAssignmentControl()`, `assignDriverToTruck()`, `hydrateVehiclesAndDrivers()`) | Verificado con `tests/operations-adapter.test.mjs` (extendido): demo adapter asigna/reasigna correctamente limpiando al chofer del vehículo anterior; adaptador real con cliente simulado cubre `listVehicleAssignments()` y las 3 llamadas (`update`+`update`+`insert`) de `assignDriverToVehicle()`, y el camino de fallback sin cliente. No requirió ninguna migración nueva — `tenant_insert_staff`/`tenant_update_staff` ya cubren `vehicle_assignments` desde `sw014_auth_rls_policies.sql`. Pendiente de verificación interactiva en staging real por el Project Owner | — |

| 25 | ~~Nada en la interfaz llamaba nunca a `startRoute()` (existía en ambos adaptadores desde antes de este ítem, sin conectar) — una ruta pasaba directo de "asignada" a "completada" sin ningún momento de arranque capturado, así que la duración de una ruta solo se podía estimar por fórmula (`shared/impact-center.js`), nunca medir de verdad~~ — **resuelto (SW-044)**: migración `route_runs.started_at`/`completed_at` (sin backfill inventado — filas previas quedan `null`, tratadas como "no medido"); ambos adaptadores sellan esos timestamps la primera vez que se alcanza ese estado, de forma idempotente; botón "Iniciar ruta"/"Marcar como completada" (detalle de ruta, admin/dispatcher, respaldo sin tocar GPS) e "Iniciar recorrido"/"Finalizar recorrido" (vista Conductor, el propio chofer — confirmado con el Project Owner tras pruebas en staging como el flujo principal: arrancar/finalizar la ruta también prende/apaga el GPS real automáticamente, un solo toque) — mismo patrón dual-write que el resto de las transiciones. El detalle de ruta muestra duración medida cuando existen ambos timestamps, o la estimación existente marcada "estimado" si no. También corrige una condición de carrera encontrada en staging: el histórico de corridas se consultaba antes de que la escritura real de `completed_at` terminara, mostrando "medido" y "sin corridas medidas" a la vez — se repite la consulta una vez confirmada la escritura | Media (resuelto) | `supabase/migrations/202607150012_sw044_route_run_timing.sql`, `shared/operations-adapter.js` (`transitionRouteRun`, `stampRouteTiming`), `frontend/app.js` (`startRouteManually()`/`completeRouteManually()` para el respaldo de admin/dispatcher, `driverStartRoute()`/`driverCompleteRoute()` para el chofer, `driverRouteLifecycleControl()`, `routeMeasuredDurationMinutes()`) | Verificado con `tests/operational-cycle.test.mjs` (extendido, contra Supabase local real — no ejecutable en este sandbox sin Docker) y `tests/operations-adapter.test.mjs` (extendido, cliente simulado: estampa en la transición correcta, nunca pisa un timestamp ya existente). Verificación interactiva en staging real por el Project Owner: confirmado el ciclo básico y la duración medida; la integración con GPS y el fix de la condición de carrera quedan pendientes de una segunda verificación interactiva | Extensión futura evaluada y descartada por ahora (confirmado con el Project Owner): tiempo promedio entre paradas medido, correlacionando GPS real con cada parada individual — más ambicioso, requiere persistir o derivar timestamps por parada, no solo por corrida completa |
| 26 | `hydrateRoutes()` (`frontend/app.js`) tomaba el `status` de una ruta hidratada desde la tabla `routes`, que solo avanza una vez —de "planned" a "assigned" (`assignToRouteRun()` en `shared/operations-adapter.js`)— y nunca más; toda transición posterior (started/in_progress/delayed/completed/verified) escribe solo en `route_runs.status`, nunca de vuelta a `routes`. Encontrado y corregido durante SW-044 porque afectaba directamente si el botón "Iniciar ruta" aparecía correctamente tras un reload — una ruta real ya iniciada o completada volvía a mostrarse como "Asignada" al recargar la página | Media (resuelto) | `frontend/app.js` (`hydrateRoutes()`) | `newRoute.status` ahora prioriza `run?.status` (el `route_run` más reciente) sobre `row.status`, cayendo a este último solo cuando no existe ningún `route_run` todavía (ruta recién creada sin vehículo asignado) | — |
| 27 | ~~Un vehículo quedaba "ocupado" para siempre después de su primera ruta~~ — **resuelto**: `truck.routeId` nunca se limpiaba al completar una ruta, así que el filtro de "vehículos disponibles para asignar" (`!truck.routeId`, usado en 4 lugares: crear ruta, asignar vehículo a una ruta existente, y sus respectivos refrescos de selects) excluía a ese vehículo para siempre — un vehículo real solo podía hacer UNA ruta en toda la vida de la app. Encontrado durante la verificación interactiva de SW-044 en staging: al intentar crear una segunda ruta y asignarle el mismo vehículo real que ya había completado una ruta antes, no aparecía como opción | Alta (resuelto) | `frontend/app.js` (nuevo `isTruckAvailable()`, reemplaza el filtro `!truck.routeId` en `renderRouteDetail()`, `renderCreateRoute()`, `refreshFleetSelects()`, `finishCreateRoute()`) | `isTruckAvailable(truck)` = sin ruta asignada, O la ruta que tiene asignada ya llegó a un estado terminal (`completed`/`verified`) — deliberadamente no limpia `truck.routeId` (se sigue usando para mostrar "Ver ruta completa", el nombre de la última ruta, etc.), solo cambia el criterio de disponibilidad. Verificado manualmente por el Project Owner en staging tras el fix — pendiente de test automatizado dedicado | — |
| 28 | ~~`assignTruckToRoute()` (`frontend/app.js`) llamaba `operationsAdapter.assignVehicle(route.id, truck.id)` sin condición~~ — **resuelto**: mismo tipo de bug que motivó los guards de SW-042/044 (`assignDriverToTruck()`, `startRouteManually()`, `completeRouteManually()`) pero nunca aplicado acá — `operationsAdapter`'s propio estado demo interno (un clon tomado al iniciar el módulo) nunca se entera de una ruta hidratada desde Supabase, así que llamarlo para una tiraba `Error: Route not found` de forma sincrónica y sin capturar, matando la función entera en silencio — "Asignar vehículo" no hacía absolutamente nada, sin ningún error visible para el usuario. Confirmado con el stack trace real en consola durante la verificación interactiva de SW-044 en staging | Alta (resuelto) | `frontend/app.js` (`assignTruckToRoute()`) | Ahora solo llama `operationsAdapter.assignVehicle()` cuando `!route.real_id` (ruta puramente local/demo) — para una ruta real, la escritura pasa exclusivamente por `realAdapter.assignVehicle()`, ya presente unas líneas más abajo en la misma función. Verificado manualmente por el Project Owner en staging tras el fix | — |

## No incluido en este registro

Elementos que el propio código ya se auto-declara como pendiente de forma explícita (p. ej. cada "REAL_NOT_RUN" en `shared/integration/status.json`) no se listan aquí como deuda individual — son decisiones de alcance de la Alpha, documentadas, no bugs ni atajos ocultos. El ítem #12 es la excepción porque hay una discrepancia entre lo prometido (README) y lo construido. "No upload real" en el portal ciudadano dejó de aplicar con SW-039 (ítem #23) — la subida de evidencias ya es real cuando hay backend conectado.
