# Registro de deuda técnica — SmartWaste Alpha

> Datos demo · no producción. Basado en `docs/CURRENT_STATE_AUDIT.md` (2026-07-29).

Severidad: **Alta** (bloquea el siguiente hito o representa riesgo de seguridad/tenancy), **Media** (debe resolverse antes de piloto real), **Baja** (limpieza / higiene, no bloqueante).

| # | Ítem | Severidad | Ubicación | Impacto | Esfuerzo estimado |
|---|---|---|---|---|---|
| 1 | ~~RLS excluye al rol `driver` de insert/update en las 13 tablas gestionadas por el loop de políticas, incluida `vehicle_positions`~~ — resuelto: `supabase/migrations/202607150006_sw020_rls_fixes.sql` ("Gap 1") agrega `driver_update_own_route_run` y `driver_insert_own_vehicle_position`/`driver_insert_own_incident`, políticas acotadas al propio `route_run`/vehículo asignado/incidencia del conductor, no un blanket grant | Alta (resuelto) | `supabase/migrations/202607150006_sw020_rls_fixes.sql:20-65` | Verificado con evidencia real: `tests/rls-adversarial.test.mjs` y `tests/telemetry-persistence.test.mjs` (ambos en CI) ejercen estas políticas contra Supabase local real, incluyendo el rechazo de escritura para un vehículo no asignado | — |
| 2 | ~~Sin política de escritura para ciudadanos anónimos en `citizen_reports`~~ — resuelto: `supabase/migrations/202607150006_sw020_rls_fixes.sql` ("Gap 2") agrega `anon_insert_citizen_report`, acotada por `municipality_is_onboarded` | Alta (resuelto) | `supabase/migrations/202607150006_sw020_rls_fixes.sql:71-99` | El portal ciudadano ya puede persistir reportes reales vía Supabase sin necesidad de un endpoint intermedio | — |
| 3 | `sw008_rls_draft.sql` habilita RLS sin políticas en 7 tablas; solo es seguro si `sw014` se aplica después en el mismo entorno **— corrección (revisión de Codex en PR #21): documentar el orden y observar corridas de CI completas en verde no hace atómicas las migraciones ni ejerce el escenario real de fallo.** Si un despliegue se interrumpe después de `sw008_rls_draft` pero antes de `sw014_auth_rls_policies`, las 7 tablas quedan con RLS habilitado y sin políticas — bloqueadas — hasta que se complete la migración pendiente | Alta | `supabase/migrations/202607150002_sw008_rls_draft.sql`, `supabase/README.md` | `supabase/README.md` documenta el orden de aplicación (mitiga el riesgo de invertir el orden manualmente), pero no existe garantía de atomicidad ni de recuperación ante un despliegue parcial — el riesgo de alta severidad sigue vigente | Medio (requiere una migración atómica que combine sw008+sw014, o un mecanismo de despliegue/rollback documentado que garantice que ambas se apliquen juntas o ninguna) |
| 4 | `createSupabaseOperationsAdapter` escribía `vehicle_id`, `driver_id` y `progress` directamente en `routes`, columnas que no existen ahí — pertenecen a `route_runs`/`vehicle_assignments` **— corrección (revisión de Codex en PR #21): el destino de escritura ya se corrigió (`assignVehicle`/`assignDriver`/`updateProgress` pasan por `assignToRouteRun()`/`transitionRouteRun()`), pero `transitionRouteRun()` solo escribe `{ status: next }` — el valor numérico de `progress` pasado a `updateProgress(routeId, progress)` se descarta silenciosamente, porque `route_runs` no tiene columna `progress`. `tests/operational-cycle.test.mjs` solo verifica el `status`, no detecta esta pérdida.** | Alta | `shared/operations-adapter.js:157` (`transitionRouteRun`) vs `supabase/migrations/202607150001_sw007_foundation.sql:11` (`route_runs`, sin columna `progress`) | El ciclo operativo real (asignar/transicionar estado) sí funciona contra el esquema migrado, pero el valor numérico de avance de una ruta nunca se persiste — cualquier UI/reporte que dependa de un `progress` real leído de Supabase mostraría datos incorrectos o ausentes | Medio (agregar columna `progress` a `route_runs` con su migración correspondiente, o remover `progress` del contrato del adapter si el modelo real solo necesita el `status`) |
| 5 | ~~4 archivos de test están nombrados como si probaran RLS/integridad/E2E reales pero solo hacen matching de texto sobre SQL/JS estático~~ — resuelto: cada archivo ahora lleva un comentario de alcance explícito al inicio aclarando que es un chequeo estático y remitiendo a la cobertura real (`tests/rls-adversarial.test.mjs`, `tests/operational-cycle.test.mjs`) | Baja (resuelto) | `tests/rls-static.test.mjs`, `tests/operations-integrity-static.test.mjs`, `tests/e2e-demo.test.mjs`, `tests/e2e-v2.test.mjs` | Riesgo de falsa confianza mitigado por el comentario; no se renombraron los archivos para no romper referencias existentes en otros docs | — |
| 6 | ~~`shared/auth-context.js` no está conectado a `frontend/`~~ — resuelto de forma opt-in por `frontend/auth-gate.js` (ver `docs/FRONTEND_LOGIN_SETUP.md`); pendiente verificación interactiva en navegador contra Supabase local | Media → Baja (pendiente de verificación real) | `frontend/auth-gate.js`, `tests/auth-gate.test.mjs` (solo lógica pura, sin navegador) | Sin configurar `window.SMARTWASTE_SUPABASE_CONFIG`, el comportamiento sigue siendo el mismo de antes (cosmético, sin login) — no es una regresión. Con la config activada, falta confirmar en navegador real que el login y el gating visual funcionan como se espera | Bajo (verificación interactiva pendiente en LabPC, ver `docs/FRONTEND_LOGIN_SETUP.md`) |
| 7 | ~~`DeviceSimulator` importado en `app.js` pero nunca instanciado~~ — resuelto: import eliminado. ~~`shared/citizen-portal.js` sin importar por `app.js`~~ — resuelto: `frontend/app.js` ahora importa `createDemoFolio`/`findSectorService`/`findIncidentStatus` y los usa en los 3 puntos donde antes había lógica inline duplicada (selector de sector, búsqueda de folio, generación de folio al reportar incidencia) | Baja (resuelto) | `frontend/app.js` (handlers de `#citizenSector`, `checkFolio`, `#incidentForm`), `shared/citizen-portal.js` | Verificado en navegador (Playwright): selector de sector, búsqueda de folio conocido/desconocido y generación secuencial de folios (`SW-FOLIO-3001`, `SW-FOLIO-3002`, ...) funcionan usando el módulo compartido como única fuente de verdad | — |
| 8 | ~~Sin CI/CD~~ — resuelto: `.github/workflows/tests.yml` (agregado en el PR de limpieza de deuda técnica) corre dos jobs en cada PR/push a `main`: `unit-tests` (cada `tests/*.test.mjs` sin Supabase, como proceso independiente) e `integration-tests` (con Supabase local real vía Docker) | Media (resuelto) | `.github/workflows/tests.yml`, `package.json` | En uso activo desde entonces en todos los PRs (#16–#20) — un commit que rompa un test ya bloquea el merge visualmente en CI | — |
| 9 | ~~`docs/DATA_MODEL_V2.md` es el único doc de dominio/datos sin banner "Datos demo · no producción"~~ — resuelto: banner agregado | Baja (resuelto) | `docs/DATA_MODEL_V2.md` | — | — |
| 10 | ~~`supabase/README.md` desactualizado — sigue diciendo "Pendiente crear migraciones..." cuando ya existen 5 migraciones~~ — resuelto: reescrito para reflejar las 6 migraciones aplicadas y verificadas en SW-020 | Baja (resuelto) | `supabase/README.md` | — | — |
| 11 | ~~Botón "Onboarding demo" en Master Admin sin handler~~ — ya no aplica: el handler existe (`frontend/app.js:179-180`, agregado en el PR de vista de supervisor, commit `a697ac9`); este ítem quedó desactualizado respecto al código | Baja (resuelto) | `frontend/app.js:179-180` | — | — |
| 12 | ~~Vista "supervisor" no existe como componente propio~~ — resuelto. ~~"Vista móvil de conductor" sin componente propio~~ — resuelto: ahora es su propia `<section id="conductor">` (`renderDriverSection()`, `frontend/app.js`), con enlace de nav dedicado (`frontend/index.html`) y gateada en `frontend/auth-gate.js`'s `SECTION_ROLES` a los mismos roles que ya veían Municipal (`municipal_admin`/`dispatcher`/`driver`). El contenido (`renderDriverMobile()`, mapa del conductor, progreso de paradas) no cambió, solo se movió de estar embebido dentro de `renderMunicipal()` a ser una sección de primer nivel | Media (resuelto) | `frontend/app.js` (`renderDriverSection()`, `showSection()`), `frontend/index.html`, `frontend/auth-gate.js` (`SECTION_ROLES`) | `tests/auth-gate.test.mjs` actualizado y en verde; verificado con Playwright que la pestaña "Conductor" cambia sola, el mapa del conductor se ve e `invalidateSize()` corre al entrar (ya no depende de entrar a Municipal) | — |
| 13 | Numeración de hitos SW-001–SW-019 no es trazable 1:1 a commits — 5 números solo existen como nombres de archivo SQL, 7 números no existen en ningún lado | Baja (proceso, no código) | Historial git completo, ver `docs/CURRENT_STATE_AUDIT.md` | Aclarado por el Project Owner: 6 PR agrupan los 19 hitos funcionales — riesgo de comunicación queda mitigado si se cita siempre "6 PR / 19 hitos funcionales" juntos | Trivial (ya reflejado en `docs/CURRENT_STATE_AUDIT.md`) |
| 14 | Supabase Realtime (`postgres_changes`) no entrega eventos de `vehicle_positions` de forma confiable — investigado en profundidad en SW-022 con acceso directo a Postgres y al log del contenedor de Realtime; el fallo resultó ser no determinístico, no un bug simple con una única causa | Alta | `supabase/migrations/202607150007_sw016_telemetry_realtime.sql`, `shared/telemetry-simulator.js` (`subscribe()`), `tests/telemetry-persistence.test.mjs`, `tests/telemetry-realtime-diagnosis.test.mjs` | SW-022 (PR #20) inspeccionó Postgres directamente (publicación `supabase_realtime`, replication slots, tabla `realtime.subscription`, grants) y confirmó que toda la configuración del lado servidor está correcta: `vehicle_positions` sí está en la publicación, el slot de replicación (`cainophile_...`, plugin `pgoutput`) está activo, y la fila de suscripción se registra con el filtro y el JWT correctos. Descarta RLS/JWT/config como causa. Pese a eso, 3 corridas de CI con la misma rama, mismo código, mismo entorno, mostraron 3 comportamientos distintos: (1) el evento se entregó correctamente esperando ~2-3s después de que el canal confirma `SUBSCRIBED`; (2) el servidor cerró el canal (`CLOSED`) ~1.3s después de ese mismo ack; (3) el canal se quedó abierto y nunca reportó ningún error, pero el evento simplemente nunca llegó. El caso (3) es clave: no hay ninguna señal observable desde el cliente para reintentar sobre ella — el contador interno `subscription_errors` del contenedor de Realtime se incrementa en los 3 casos casi al mismo tiempo (~1-1.3s tras el join), pero solo a veces eso se traduce en un `CLOSED` visible para el cliente. `shared/telemetry-simulator.js`'s `subscribe()` reintenta con backoff (1s/2s/4s) ante fallos observables como mitigación de mejor esfuerzo, pero no puede resolver el caso (3) por diseño — se documenta así en el propio código. La aserción de entrega de Realtime en `tests/telemetry-persistence.test.mjs` se mantiene fuera (no bloqueante); `shared/integration/status.json`'s `sw016.realtime` se mantiene `REAL_NOT_RUN`. `tests/telemetry-realtime-diagnosis.test.mjs` queda en CI como diagnóstico permanente (no bloqueante) para seguir recolectando evidencia en corridas futuras | Alto (parece ser un problema de la propia infraestructura self-hosted de Realtime en esta versión, `v2.113.4` — no resoluble solo con cambios de configuración o lógica del lado cliente; requeriría investigar el código fuente de Realtime o probar una versión distinta del contenedor) |
| 15 | El contenedor de auditoría/Claude Code no tiene salida de red hacia el CDN de blobs de Docker Hub (`production.cloudfront.docker.com`) — el proxy de egreso de la organización responde `403 Forbidden` a ese host específico. `docker pull` puede resolver el manifest (`registry-1.docker.io` funciona) pero falla al bajar las capas de imagen. Confirmado con `docker info`/`docker pull hello-world` y con `curl $HTTPS_PROXY/__agentproxy/status` (`recentRelayFailures`: `connect_rejected`, `gateway answered 403 to CONNECT`, host `production.cloudfront.docker.com:443`) | Alta | Entorno del contenedor (no es código del repo); ver `/root/.ccr/README.md`, sección "403 / 407 from the proxy" | Bloquea `supabase start` (Postgres/GoTrue/PostgREST/Kong son imágenes Docker) desde este contenedor — SW-016 (telemetría/Realtime) y SW-017 (E2E frontend real contra Supabase local) no se pueden ejecutar aquí, igual que SW-020 tuvo que ejecutarse en LabPC. No es un problema de configuración de Docker/CA — es una denegación de política de red explícita; no reintentar ni rodear, según el README del proxy | No accionable desde el repo/código. Requiere que alguien con acceso a la política de egreso del workspace habilite `production.cloudfront.docker.com` (y probablemente otros hosts de CDN de imágenes) en el proxy, o que el hito se siga ejecutando en LabPC como hasta ahora |
| 16 | ~~SW-027 ("Crear ruta") conecta una ruta recién creada mutando en runtime el diccionario `routePaths`~~ — resuelto por completo. `shared/telemetry-simulator.js`'s `DeviceSimulator` ahora acepta un `getPath` opcional en el constructor (`{ getPath: () => [...] }`), llamado de forma perezosa en cada `emit()` en vez de leído una sola vez — sin `getPath`, sigue el mismo fallback de siempre (`routePaths`/`trucks`), así que ningún llamador existente cambia de comportamiento. `frontend/app.js` pasa `getPath: () => routeGeometry(truck.routeId)` en sus dos puntos de construcción (seeding inicial y `registerDriverSimulator()`), y también se corrigió `startSimulation()`'s tick, que todavía leía `routePaths[truck.routeId]` directamente (se me había pasado en SW-033). Con esto, `finishCreateRoute()` ya no necesita mutar `routePaths` en absoluto — se eliminó esa línea | Baja (resuelto) | `shared/telemetry-simulator.js` (`DeviceSimulator`), `frontend/app.js` (`registerDriverSimulator()`, seeding de `driverSimulators`, `startSimulation()`) | Verificado: `tests/telemetry-simulator.test.mjs` cubre que `getPath()` gana sobre el fallback y se llama en cada `emit()`, no solo al construir; suite completa sin regresiones; Playwright confirma que dibujar una ruta, asignarle vehículo e iniciar la simulación funciona sin errores | — |
| 17 | ~~SW-034 conectó las escrituras de `frontend/app.js` a Supabase real, pero no hidrataba datos reales preexistentes hacia la página en ejecución~~ — resuelto en dos fases (SW-035). **Fase A:** `hydrateVehiclesAndDrivers()` trae `listVehicles()`/`listDrivers()` y los agrega a `trucks`/`drivers`. **Fase B:** `hydrateRoutes()` trae `listRoutes()` + el nuevo `listRouteRuns()` (`shared/operations-adapter.js` — no existía ningún método que leyera `route_runs` en bloque, solo transicionarlos uno por uno), cruza cada ruta con su `route_run` más reciente por `route_id` para resolver vehículo/chofer/progreso reales (routes en sí no tiene esas columnas — viven en `route_runs`), y siembra geometría/paradas en el adaptador demo con el mismo patrón que ya siembra las 5 rutas demo al inicio del módulo, para que `routeGeometry()`/`drawMapLayers()`/la vista del conductor funcionen igual que con una ruta demo. Una ruta hidratada con vehículo asignado también recibe `simState`/`driverSimulators` para poder participar de la simulación | Media (resuelto) | `frontend/app.js` (`hydrateVehiclesAndDrivers()`, `hydrateRoutes()`, `bootstrapRealBackend()`); `shared/operations-adapter.js` (`listRouteRuns()`, nuevo) | Verificado con un cliente Supabase simulado en Playwright: una ruta, un vehículo y un chofer sembrados directamente en el mock (nunca creados en la sesión del navegador) aparecen al cargar la página, con la unidad asignada y el progreso reales correctos (leídos de `route_runs`), sin errores de consola. `tests/operations-adapter.test.mjs` cubre `listRouteRuns()` con un cliente simulado | — |

| 18 | "Trazado inteligente" (`finishCreateRoute()` en `frontend/app.js`, `shared/osrm-routing.js`) usa el servidor demo público de OSRM (`router.project-osrm.org`) para ajustar el trazo dibujado a calles reales — elegido explícitamente por el Project Owner para un piloto de 3-5 camiones. Es gratuito y no requiere credenciales, pero es un servicio de evaluación: sin SLA, con límites de tasa no documentados públicamente, no pensado para carga de producción | Media | `shared/osrm-routing.js`, `frontend/app.js` (`finishCreateRoute()`) | Si el servidor demo público limita o rechaza peticiones bajo uso real, la función cae automáticamente al trazo en línea recta (fallback ya implementado, nunca bloquea la creación de rutas — regla 5); el mensaje de estado en la UI ya distingue ambos casos | Bajo mientras el piloto sea pequeño; si el municipio crece o el uso se vuelve intensivo, migrar a un servidor OSRM propio o a un proveedor con SLA (ver comparación de servicios de ruteo hecha en la sesión de planeación de este hito) |

| 19 | El optimizador de orden de paradas (`shared/route-optimizer.js`, roadmap ítem 2, opt-in vía el checkbox "Optimizar orden de paradas" en "Crear ruta") usa distancia en línea recta (haversine) entre puntos, no la red vial real — no considera calles de un solo sentido, restricciones de giro, ni el trazo que luego calcula OSRM. El orden se decide *antes* de llamar a OSRM (por diseño: OSRM ajusta la geometría del orden ya decidido a calles reales, no vuelve a decidir el orden), así que el resultado puede no ser el orden verdaderamente más corto una vez ajustado a calles reales. Es además una heurística (nearest-neighbor + 2-opt), no una solución exacta de TSP — adecuada para las 2-30 paradas dibujadas a mano por un despachador, no garantiza el óptimo global | Baja | `shared/route-optimizer.js`, `frontend/app.js` (`finishCreateRoute()`, checkbox `#createRouteOptimize`) | El trazo sigue siendo válido y usable en cualquier caso (la optimización es opt-in y desactivada por defecto — regla 5); es una mejora de eficiencia de ruta, no de correctitud, y el despachador puede desactivarla si el orden manual es intencional (p. ej. por calles de un solo sentido) | Bajo si en el futuro se quisiera reoptimizar contra distancias reales de calle (usar el servicio Trip/Table de OSRM en vez de solo Route), pero fuera de alcance de este hito |

## No incluido en este registro

Elementos que el propio código ya se auto-declara como pendiente de forma explícita (p. ej. cada "REAL_NOT_RUN" en `shared/integration/status.json`, o cada "no upload real" en el portal ciudadano) no se listan aquí como deuda individual — son decisiones de alcance de la Alpha, documentadas, no bugs ni atajos ocultos. El ítem #12 es la excepción porque hay una discrepancia entre lo prometido (README) y lo construido.
