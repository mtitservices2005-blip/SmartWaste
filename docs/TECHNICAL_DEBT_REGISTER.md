# Registro de deuda técnica — SmartWaste Alpha

> Datos demo · no producción. Basado en `docs/CURRENT_STATE_AUDIT.md` (2026-07-29).

Severidad: **Alta** (bloquea el siguiente hito o representa riesgo de seguridad/tenancy), **Media** (debe resolverse antes de piloto real), **Baja** (limpieza / higiene, no bloqueante).

| # | Ítem | Severidad | Ubicación | Impacto | Esfuerzo estimado |
|---|---|---|---|---|---|
| 1 | RLS excluye al rol `driver` de insert/update en las 13 tablas gestionadas por el loop de políticas, incluida `vehicle_positions` | Alta | `supabase/migrations/202607150004_sw014_auth_rls_policies.sql:28,30` | Un conductor autenticado real no podría iniciar rutas, actualizar progreso ni enviar telemetría — rompe el flujo operativo central en cuanto se active Supabase real | Pequeño (añadir `'driver'` al array de roles permitidos, con columnas/condiciones que limiten qué puede tocar de cada tabla; requiere diseño, no solo el flag) |
| 2 | Sin política de escritura para ciudadanos anónimos en `citizen_reports` | Alta | `supabase/migrations/202607150004_sw014_auth_rls_policies.sql:24-31` | El portal ciudadano no podría persistir reportes reales vía Supabase — toda la feature de "Portal ciudadano" quedaría bloqueada en producción | Medio (definir rol/política `anon insert` con validaciones anti-abuso, o un endpoint de servidor intermedio) |
| 3 | `sw008_rls_draft.sql` habilita RLS sin políticas en 7 tablas; solo es seguro si `sw014` se aplica después en el mismo entorno, y no hay evidencia de que las 5 migraciones se hayan corrido juntas nunca | Alta | `supabase/migrations/202607150002_sw008_rls_draft.sql` | Riesgo de aplicar migraciones fuera de orden y dejar tablas completamente bloqueadas (o, si se editan mal, completamente abiertas) | Bajo (documentar orden obligatorio; considerar colapsar sw008+sw014 en una sola migración de RLS al activar Supabase) |
| 4 | 4 archivos de test están nombrados como si probaran RLS/integridad/E2E reales pero solo hacen matching de texto sobre SQL/JS estático | Media | `tests/rls-static.test.mjs`, `tests/operations-integrity-static.test.mjs`, `tests/e2e-demo.test.mjs`, `tests/e2e-v2.test.mjs` | Riesgo de falsa confianza: "los tests de RLS pasan" no significa que el aislamiento multiinstitución funcione. `rls-static.test.mjs:4` usa una regex tan permisiva que matchea con solo el nombre de la tabla | Bajo (renombrar a `*-lint.test.mjs` o similar y/o añadir un comentario explícito de alcance; el trabajo real es construir tests de integración contra Supabase local en el próximo hito) |
| 5 | `shared/auth-context.js` no está conectado a `frontend/` — no hay pantalla de login ni gating por rol | Media | `frontend/app.js` (sin imports de `auth-context.js`); confirmado en `docs/ROLE_PERMISSION_MATRIX.md:11` | Control de acceso hoy es cosmético; cualquier visitante ve todos los paneles (municipal, ciudadano, master admin) sin autenticación | Medio–Alto (requiere UI de login + routing con gating, además de activar Supabase Auth) |
| 6 | `DeviceSimulator` importado en `app.js` pero nunca instanciado — código muerto desde la perspectiva del frontend | Baja | `frontend/app.js:3` vs `shared/telemetry-simulator.js:6-18` | Confunde a quien lea el código pensando que el simulador formal alimenta el mapa; en realidad el mapa usa lógica de simulación duplicada e inline | Bajo (eliminar el import muerto, o migrar `app.js` a usar `DeviceSimulator` como fuente única de verdad) |
| 7 | Sin CI/CD — no existe `.github/workflows/` ni `package.json` en ningún punto del repo | Media | raíz del repo | Los 13 tests locales solo se ejecutan manualmente; nada impide un commit que rompa un test | Bajo–Medio (agregar un workflow simple que corra `node tests/*.test.mjs`) |
| 8 | `docs/DATA_MODEL_V2.md` es el único doc de dominio/datos sin banner "Datos demo · no producción" | Baja | `docs/DATA_MODEL_V2.md` | Riesgo de que alguien lea solo ese archivo y asuma que describe un esquema ya vivo, cuando en realidad son tablas de migraciones nunca aplicadas | Trivial (agregar la línea de banner, igual que en `DATA_MODEL.md`, `ALPHA_READINESS.md`, etc.) |
| 9 | `supabase/README.md` desactualizado — sigue diciendo "Pendiente crear migraciones..." cuando ya existen 5 migraciones | Baja | `supabase/README.md` | Documentación engañosa para quien entre por ese README primero | Trivial |
| 10 | Botón "Onboarding demo" en Master Admin sin handler | Baja | `frontend/app.js:116` | UI inerte que puede parecer un bug a un demo en vivo | Trivial |
| 11 | Vista "supervisor" y "vista móvil de conductor" no existen como componentes propios pese a estar en el README/roadmap como features del piloto | Media | `README.md` (lista de features), sin contraparte en `frontend/app.js` | Brecha entre lo que se promete al Project Owner/municipio y lo que hay hoy en pantalla | Medio (construir ambas vistas es trabajo de producto, no deuda de "arreglo rápido") |
| 12 | Numeración de hitos SW-001–SW-019 no es trazable 1:1 a commits — 5 números solo existen como nombres de archivo SQL, 7 números no existen en ningún lado | Baja (proceso, no código) | Historial git completo, ver `docs/CURRENT_STATE_AUDIT.md` | Riesgo de comunicación si la numeración se usa externamente como "19 incrementos revisados" | Trivial (aclarar convención en `CLAUDE.md`/README, ya incorporado en las reglas nuevas) |

## No incluido en este registro

Elementos que el propio código ya se auto-declara como pendiente de forma explícita (p. ej. cada "REAL_NOT_RUN" en `shared/integration/status.json`, o cada "no upload real" en el portal ciudadano) no se listan aquí como deuda individual — son decisiones de alcance de la Alpha, documentadas, no bugs ni atajos ocultos. El ítem #11 es la excepción porque hay una discrepancia entre lo prometido (README) y lo construido.
