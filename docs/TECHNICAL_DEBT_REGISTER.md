# Registro de deuda técnica — SmartWaste Alpha

> Datos demo · no producción. Basado en `docs/CURRENT_STATE_AUDIT.md` (2026-07-29).

Severidad: **Alta** (bloquea el siguiente hito o representa riesgo de seguridad/tenancy), **Media** (debe resolverse antes de piloto real), **Baja** (limpieza / higiene, no bloqueante).

| # | Ítem | Severidad | Ubicación | Impacto | Esfuerzo estimado |
|---|---|---|---|---|---|
| 1 | RLS excluye al rol `driver` de insert/update en las 13 tablas gestionadas por el loop de políticas, incluida `vehicle_positions` | Alta | `supabase/migrations/202607150004_sw014_auth_rls_policies.sql:28,30` | Un conductor autenticado real no podría iniciar rutas, actualizar progreso ni enviar telemetría — rompe el flujo operativo central en cuanto se active Supabase real | Pequeño (añadir `'driver'` al array de roles permitidos, con columnas/condiciones que limiten qué puede tocar de cada tabla; requiere diseño, no solo el flag) |
| 2 | Sin política de escritura para ciudadanos anónimos en `citizen_reports` | Alta | `supabase/migrations/202607150004_sw014_auth_rls_policies.sql:24-31` | El portal ciudadano no podría persistir reportes reales vía Supabase — toda la feature de "Portal ciudadano" quedaría bloqueada en producción | Medio (definir rol/política `anon insert` con validaciones anti-abuso, o un endpoint de servidor intermedio) |
| 3 | `sw008_rls_draft.sql` habilita RLS sin políticas en 7 tablas; solo es seguro si `sw014` se aplica después en el mismo entorno, y no hay evidencia de que las 5 migraciones se hayan corrido juntas nunca | Alta | `supabase/migrations/202607150002_sw008_rls_draft.sql` | Riesgo de aplicar migraciones fuera de orden y dejar tablas completamente bloqueadas (o, si se editan mal, completamente abiertas) | Bajo (documentar orden obligatorio; considerar colapsar sw008+sw014 en una sola migración de RLS al activar Supabase) |
| 4 | `createSupabaseOperationsAdapter` escribe `vehicle_id`, `driver_id` y `progress` directamente en `routes`, columnas que no existen ahí — pertenecen a `route_runs`/`vehicle_assignments` (hallazgo de revisión del PR #7, confirmado por lectura del esquema) | Alta | `shared/operations-adapter.js:72,74,76` vs `supabase/migrations/202607150001_sw007_foundation.sql:9,11,12` | El adapter Supabase no puede ejecutar el ciclo real (asignar/progresar) contra el esquema migrado sin reconciliación; bloquea directamente el objetivo de SW-020 de ejercer un ciclo operativo real | Medio (alinear el adapter al modelo `routes`→`route_runs`→`vehicle_assignments`, preservando la máquina de estados; no tocar el esquema para acomodar el adapter tal como está) |
| 5 | ~~4 archivos de test están nombrados como si probaran RLS/integridad/E2E reales pero solo hacen matching de texto sobre SQL/JS estático~~ — resuelto (SW-021): cada archivo ahora tiene un comentario de alcance explícito al inicio aclarando que no corre contra Postgres/navegador real y apuntando al test real equivalente (`rls-adversarial`, `operational-cycle`). No se renombraron (evita romper referencias cruzadas en docs de auditoría con fecha) | Baja (resuelto, ver comentarios en cada archivo) | `tests/rls-static.test.mjs`, `tests/operations-integrity-static.test.mjs`, `tests/e2e-demo.test.mjs`, `tests/e2e-v2.test.mjs` | — | — |
| 6 | ~~`shared/auth-context.js` no está conectado a `frontend/`~~ — resuelto de forma opt-in por `frontend/auth-gate.js` (ver `docs/FRONTEND_LOGIN_SETUP.md`); pendiente verificación interactiva en navegador contra Supabase local | Media → Baja (pendiente de verificación real) | `frontend/auth-gate.js`, `tests/auth-gate.test.mjs` (solo lógica pura, sin navegador) | Sin configurar `window.SMARTWASTE_SUPABASE_CONFIG`, el comportamiento sigue siendo el mismo de antes (cosmético, sin login) — no es una regresión. Con la config activada, falta confirmar en navegador real que el login y el gating visual funcionan como se espera | Bajo (verificación interactiva pendiente en LabPC, ver `docs/FRONTEND_LOGIN_SETUP.md`) |
| 7 | `DeviceSimulator` importado en `app.js` pero nunca instanciado — código muerto desde la perspectiva del frontend. Adicionalmente, `shared/citizen-portal.js` tampoco está importado por `app.js` (el portal ciudadano de la UI usa lógica inline duplicada, no `createDemoFolio`/`findSectorService`/`findIncidentStatus`) | Baja | `frontend/app.js:3` vs `shared/telemetry-simulator.js:6-18`; `shared/citizen-portal.js` (sin referencias en `frontend/app.js`) | Confunde a quien lea el código pensando que estos módulos alimentan la UI; en realidad el mapa y el portal ciudadano usan lógica duplicada e inline | Bajo (eliminar imports muertos, o migrar `app.js` a usar estos módulos como fuente única de verdad) |
| 8 | Sin CI/CD — no existe `.github/workflows/` ni `package.json` en ningún punto del repo | Media | raíz del repo | Los 13 tests locales solo se ejecutan manualmente, y solo si se invocan como procesos separados (`node tests/*.test.mjs` sin loop no los ejecuta todos); nada impide un commit que rompa un test | Bajo–Medio (agregar un workflow simple que corra cada archivo de `tests/` como proceso independiente) |
| 9 | ~~`docs/DATA_MODEL_V2.md` sin banner "Datos demo · no producción"~~ — resuelto (SW-021) | Baja (resuelto) | `docs/DATA_MODEL_V2.md` | — | — |
| 10 | ~~`supabase/README.md` desactualizado~~ — resuelto (SW-021): ahora refleja las 6 migraciones aplicadas y verificadas en SW-020, con instrucciones para levantar local | Baja (resuelto) | `supabase/README.md` | — | — |
| 11 | ~~Botón "Onboarding demo" en Master Admin sin handler~~ — resuelto (feature/supervisor-view) | Baja (resuelto) | `frontend/app.js` | — | — |
| 12 | ~~Vista "supervisor" y "vista móvil de conductor" no existen como componentes propios~~ — resuelto: supervisor en `feature/supervisor-view`, conductor en SW-021 (`renderConductor()`, sección `#conductor` gateada por rol en `frontend/auth-gate.js`). Ambas siguen operando sobre datos demo en memoria, no contra Supabase real — eso es una limitación compartida por *toda* la UI hoy (`operationsAdapter` está importado en `app.js` pero no conectado a ninguna sección todavía), no específica de estas dos vistas | Baja (resuelto para el alcance de UI; conectar cualquier sección a Supabase real es trabajo futuro, no deuda de esta) | `frontend/app.js`, `frontend/auth-gate.js` | — | — |
| 13 | Numeración de hitos SW-001–SW-019 no es trazable 1:1 a commits — 5 números solo existen como nombres de archivo SQL, 7 números no existen en ningún lado | Baja (proceso, no código) | Historial git completo, ver `docs/CURRENT_STATE_AUDIT.md` | Aclarado por el Project Owner: 6 PR agrupan los 19 hitos funcionales — riesgo de comunicación queda mitigado si se cita siempre "6 PR / 19 hitos funcionales" juntos | Trivial (ya reflejado en `docs/CURRENT_STATE_AUDIT.md`) |

## No incluido en este registro

Elementos que el propio código ya se auto-declara como pendiente de forma explícita (p. ej. cada "REAL_NOT_RUN" en `shared/integration/status.json`, o cada "no upload real" en el portal ciudadano) no se listan aquí como deuda individual — son decisiones de alcance de la Alpha, documentadas, no bugs ni atajos ocultos. El ítem #12 es la excepción porque hay una discrepancia entre lo prometido (README) y lo construido.
