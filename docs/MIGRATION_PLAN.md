# MIGRATION PLAN

A local draft migration exists at `supabase/migrations/202607150001_sw007_foundation.sql`. It was not pushed or applied remotely. Next steps: review constraints, add RLS, seed non-official demo data, test locally, then promote through approved environments only.
